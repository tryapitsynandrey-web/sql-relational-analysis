-- FileName: 01_create_schema.sql — Sets up the custom schema for the Olist dataset.

-- Standard practice is to isolate the data within a specific schema rather than public
CREATE SCHEMA IF NOT EXISTS olist;
